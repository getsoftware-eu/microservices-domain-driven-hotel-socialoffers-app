apiVersion: v1
kind: ConfigMap
metadata:
  name: postgres-config #config c8s pod
data: #like 'environment' in docker compose
  POSTGRES_DB: postgres 
  POSTGRES_USER: eu_user 
  POSTGRES_PASSWORD: postgres_password 