apiVersion: v1
kind: ConfigMap
metadata:
  name: rabbitmq-config
data: #like 'environment' in docker compose
  POSTGRES_DB: rabbitmq 
  POSTGRES_USER: rabbitmq 
  POSTGRES_PASSWORD: rabbitmq_password 