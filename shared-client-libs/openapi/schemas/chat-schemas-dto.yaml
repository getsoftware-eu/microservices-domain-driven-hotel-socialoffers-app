components:
  schemas:
    ChatMsgDTO:
      type: object
      properties:
        id:
          type: integer
        message:
          type: string
        senderId:
          type: integer
        recipientId:
          type: integer
        timestamp:
          type: string
          format: date-time
    